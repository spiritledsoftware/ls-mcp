---
"@spiritledsoftware/ls-mcp": patch
---

Improve MCP tool ergonomics with `lsp_list_servers`, partial standard-tool acquisition results, `strict` mode, and safer `workspace_symbols` server targeting.
